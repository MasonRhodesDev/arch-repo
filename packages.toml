schema = 1
owner = "MasonRhodesDev"

[[release]]
repository = "hyprstate"
packages = ["hyprstate", "hyprstate-debug"]

[[release]]
repository = "dials"
packages = ["dials", "dials-debug"]

[[release]]
repository = "sni-watcher"
packages = ["sni-watcher", "sni-watcher-debug"]

[[release]]
repository = "logind-idle-control"
packages = ["logind-idle-control", "logind-idle-control-debug"]

[[release]]
repository = "linux-multi-theme-toggle"
packages = ["lmtt", "lmtt-debug"]

[[release]]
repository = "wayland-voice-dictation"
packages = ["wayland-voice-dictation", "wayland-voice-dictation-debug"]

[[release]]
repository = "couchcord"
packages = ["couchcord", "couchcord-debug"]

[[release]]
repository = "deck-tenant"
packages = ["deck-tenant"]

[[release]]
repository = "greetd_game_mode"
packages = ["game-mode", "game-mode-debug"]

[[release]]
repository = "hypr-DE"
packages = ["hypr-de", "hypr-de-gaming"]

[[release]]
repository = "hypr-de-extras"
packages = ["overskride", "overskride-debug"]

[[release]]
repository = "waybar-workspace-buttons"
packages = ["waybar-workspace-buttons", "waybar-workspace-buttons-debug", "hyprland-workspace-zones"]

[[release]]
repository = "vigil"
packages = ["vigil", "vigil-debug"]

# The hyprland-git channel: lockstep pair from this repo's own git-builds
# releases (see git-builds.yml). Version = git describe of hyprwm main.
[[release]]
repository = "arch-repo"
packages = ["hyprland-git", "hyprland-workspace-zones-git"]
